name: Scoring

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:
  
jobs:
  scoring:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      OSN_KEY: ${{ secrets.OSN_KEY }}
      OSN_SECRET: ${{ secrets.OSN_SECRET }}
    container:
      image: ghcr.io/boettiger-lab/k8s@sha256:fd02edfb4318afe022f8a732a99d93df7127dadb2bb4e208e36a86f96c2f45c6
      options: --user root
    steps:

      - name: cleanup disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc
          df -h

      - uses: actions/checkout@v4

      - name: install
        run: |
          R -e "options(repos = c(eco4cast = 'https://eco4cast.r-universe.dev', CRAN = 'https://cloud.r-project.org')); install.packages(c('rlang', 'bench', 'duckdbfs', 'minioclient', 'callr', 'score4cast'))"
          
      - name: scoring-prep
        run: r scoring/prep-scoring.R

      - name: scoring
        run: r scoring/scoring.R
