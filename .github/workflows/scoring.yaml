name: Scoring

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:
  
jobs:
  scoring:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      OSN_KEY: ${{ secrets.OSN_KEY }}
      OSN_SECRET: ${{ secrets.OSN_SECRET }}
    container:
      image: ghcr.io/boettiger-lab/k8s@sha256:fd02edfb4318afe022f8a732a99d93df7127dadb2bb4e208e36a86f96c2f45c6
      options: --user root
    steps:

      - name: cleanup disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc
          df -h

      - uses: actions/checkout@v4

      - name: install
        run: |
          sudo sed -i '/r2u/d' /etc/apt/sources.list.d/*.list 2>/dev/null || true
          R -e "update.packages(ask=FALSE, checkBuilt=TRUE, lib='/usr/lib/R/site-library', repos='https://cloud.r-project.org')"
          R -e "options(repos = c(eco4cast = 'https://eco4cast.r-universe.dev', CRAN = 'https://cloud.r-project.org')); install.packages(c('bench', 'duckdbfs', 'minioclient', 'callr'))"
          R -e "remotes::install_github('eco4cast/score4cast')"
          
      - name: scoring-prep
        run: r scoring/prep-scoring.R

      - name: scoring
        run: r scoring/scoring.R
